export const mapScale = 5;
export const systemDistanceMultiplier = 25;

export let universeCoordinates = {
    minX: 0,
    minY: 0,
    maxX: 0,
    maxY: 0,
}
export let systemCoordinates = {
    minX: 0,
    minY: 0
}
export const scale = {
    universe: 1
}

export const gameWidth = window.innerWidth ?? 1920
export const gameHeight = window.innerHeight ?? 1080