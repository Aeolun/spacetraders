[tool.poetry]
name = "app"
version = "0.1.0"
description = ""
authors = ["Bart Riepe <bart@serial-experiments.com>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "~3.12.0"
ortools = "^9.7.2996"
flask = "^3.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
